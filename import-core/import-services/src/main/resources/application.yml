# app settings

debug : true

spring:
  thymeleaf:
    cache: false

---
spring:
  profiles: cloud

---
spring:
  profiles: default
  dataSource:
    driverClassName : org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/import_tool
    password : 
    username : sa
    
  jpa:
    database : H2
    hibernate:
      ddl-auto : none

