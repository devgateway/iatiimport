# app settings

debug : true

spring:
  thymeleaf:
    cache: false

---
spring:
  profiles: cloud

---
spring:
  profiles: default
  dataSource:
    driverClassName : org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/import_tool
    password : m0n1t0r
    username : amp
    
  jpa:
    database : POSTGRESQL
    hibernate:
      ddl-auto : update
      show-sql: true

